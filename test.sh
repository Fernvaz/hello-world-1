cat <<ROF > test.sh                                                     #Elects to end the file once "EOF" is typed.
#!/bin/bash
echo "This is the best way to echo in a config file, because WYSIWYG"
EOF
